import styles from '../Container/Container.module.css';

function Content({title, balance}) {
    return (
        <div className="content-container">
            
        </div>
    )
}

export default Content;